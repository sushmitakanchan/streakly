{
  "version": 2,
  "buildCommand": "(cd backend && npm install && npm run build) && (cd frontend && npm install && npm run build)",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/v1/:path*", "destination": "/api/index.js" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
